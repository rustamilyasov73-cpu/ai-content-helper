// Каркас HTTP-сервиса AgroParts для 1С:Бухгалтерия предприятия.
// Создайте HTTP-сервис "agroparts", корневой URL: agroparts
// Шаблоны URL:
//   ping          GET
//   nomenclature  GET
//   orders        POST
//
// В модуле HTTP-сервиса разместите обработчики ниже и доработайте
// под вашу схему справочников/документов.

Функция PingGET(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки("{""ok"":true,""version"":""1.0""}");
	Возврат Ответ;
КонецФункции

Функция NomenclatureGET(Запрос)
	// TODO: выборка из Справочник.Номенклатура + регистр цен/остатков
	Массив = Новый Массив;
	// Пример элемента:
	// Элемент = Новый Структура;
	// Элемент.Вставить("sku", "RE507922");
	// Элемент.Вставить("name", "Фильтр масляный");
	// Элемент.Вставить("category", "filters");
	// Элемент.Вставить("brand", "John Deere");
	// Элемент.Вставить("compatible", "6R,7R");
	// Элемент.Вставить("price", 2890);
	// Элемент.Вставить("stock", 24);
	// Элемент.Вставить("unit", "шт");
	// Элемент.Вставить("description", "");
	// Массив.Добавить(Элемент);

	Результат = Новый Структура("items", Массив);
	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись, Результат);
	Тело = Запись.Закрыть();

	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(Тело);
	Возврат Ответ;
КонецФункции

Функция OrdersPOST(Запрос)
	Тело = Запрос.ПолучитьТелоКакСтроку();
	Чтение = Новый ЧтениеJSON;
	Чтение.УстановитьСтроку(Тело);
	Данные = ПрочитатьJSON(Чтение);

	// TODO: найти/создать контрагента по телефону Данные.phone
	// TODO: создать Документ.ЗаказКлиента / РеализацияТоваровУслуг
	// TODO: заполнить табличную часть по Данные.items (поиск номенклатуры по sku)

	НомерДокумента = "00000001";

	Результат = Новый Структура;
	Результат.Вставить("ok", Истина);
	Результат.Вставить("number", НомерДокумента);
	Результат.Вставить("ref", "");
	Результат.Вставить("message", "Заказ создан");

	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись, Результат);
	ТелоОтвета = Запись.Закрыть();

	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(ТелоОтвета);
	Возврат Ответ;
КонецФункции
